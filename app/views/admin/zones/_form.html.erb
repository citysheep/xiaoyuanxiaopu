<%= simple_form_for([:admin, @zone], :html => { :class => 'form-horizontal' }) do |f| %>
  <% if @zone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zone.errors.count, 'error') %> prohibited this zone from being saved:</h2>
      <ul>
      <% @zone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.input :city_id, :label => '城市', :collection => City.sort_by_name, :selected => @zone.city ? @zone.city.id : 1, :input_html => {:class=> 'select-block span3'}  %>
  <%= f.input :name, :label => '行政区名称'%>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary', :value => '确定' %>
  </div>
<% end %>
